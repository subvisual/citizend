[profile.default]
src = 'contracts'
out = 'out'
libs = ['lib', 'node_modules']
test = 'test'
cache_path  = 'cache_forge'

[rpc_endpoints]
sepolia = "${ETH_RPC_URL}"

[etherscan]
sepolia = { key = "${ETHERSCAN_API_KEY}"}
