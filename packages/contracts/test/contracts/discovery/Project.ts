describe("Project", () => {
  describe("addPeoplesPoolInvestment", () => {
    it("includes a new investment record");
    it("can only be called by the batch role");
  });

  describe("addStakersPoolInvestment", () => {
    it("includes a new investment record");
    it("can only be called by the batch role");
  });
});
