/**
 * Module dependencies.
 */

import styled from 'styled-components';

/**
 * Export `Separator` styled component.
 */

export const Separator = styled.div`
  background-color: currentColor;
  height: 1px;
  opacity: 0.3;
`;
